version: "2"
plugins:
  duplication:
    enabled: true
    config:
      languages:
      - ruby:
  fixme:
    enabled: true
  rubocop:
    enabled: true
    channel: rubocop-1-56-3
exclude_patterns:
  - "spec/"
