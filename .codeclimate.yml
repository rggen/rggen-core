engines:
  duplication:
    enabled: true
    config:
      languages:
      - ruby:
  fixme:
    enabled: true
  reek:
    enabled: true
  rubocop:
    enabled: true
    channel: rubocop-0-48
ratings:
   paths:
    - "lib/**/*"
exclude_paths:
  - "spec/"
